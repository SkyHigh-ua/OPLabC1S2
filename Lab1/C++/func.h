#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

void inputfile(string, int);
void croutfile(string, string, string);
vector<string> matchlines(string, string);
vector<string> lstlines(string);
int filelen(string);
void printfile(string);